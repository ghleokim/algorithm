import sys
sys.stdin = open('input.txt', 'r')

def checkLeft(chosen, startIndex, current):
    left = []
    for num in chosen:
        if


for T in range(int(input())):
    n, *s = int(input()), *map(int, input().split())

    chosen = [0] * n
    length = 0






    # print('#{0} {1}'.format(T,*result))